rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /people/{uid} {
      allow read: if true;
      allow write: if request.resource.data.keys().hasOnly(['name','books','rewardLabels','updatedAt'])
                   && request.resource.size() < 900000;
    }
  }
}
